full stack application by the name of Code/day3FS 

using axios instead if fetch for backend data cause it provides production level features, library written specially for web requests
```
npm i axios
```

one of the usecase of axios is it will handle the data no need to handle if its json string whatever

almost all process could be found in commit history, messages